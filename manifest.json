{
  "manifest_version": 3,
  "name": "GoDark",
  "version": "1.0",
  "description": "Force dark mode on websites that don't support it.",
  "permissions": ["scripting", "activeTab"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_title": "GoDark - Click to enable dark mode"
  }
}
